{
  "buildCommand": "sh -c 'cd frontend && npm install && npm run build'",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
